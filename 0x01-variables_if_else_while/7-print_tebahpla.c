#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Smile in the mirror
 *
 *
 * Return: Alwaays 0 (Success)
 */

int main(void)
{
	char alph;

	for (alph = 'z'; alph >= 'z'; alph++)

	putchar(alph);
	putchar('\n');
	return (0);
}
